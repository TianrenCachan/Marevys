# MARÉVYS PARIS — RC21 塔罗与本命星盘

English / Français / 简体中文

本版以塔罗和本命星盘为两个主要入口。保留品牌视觉、可选器物系列和以前的符文／易经记录，暂时隐藏符文与易经的新阅读入口。没有新增会员计费、支付、真实账户或云端档案功能。

## 本版功能

- 完整 78 张塔罗牌：一张、三张、五张牌阵；抽牌前确定问题、背景、关注期限和是否使用逆位。
- 服务器使用安全随机数洗牌；盲选、自动选牌和逐张翻牌共享同一副已洗好的牌。生产环境通过 Redis 固定牌阵，重复或并发请求不能换牌。
- 深入解读结合牌、正逆位、固定牌位、相关资料摘要和用户背景，输出综合分析、牌间关系、不确定性与行动建议。
- 原牌阵可追问；每次会话只能补一张澄清牌。补牌从原牌堆取出，不覆盖或替换原牌。
- 本命星盘由 Astronomy Engine 计算，采用回归黄道、地心位置和整宫制；AI 仅解释已计算的事实。
- 出生时间未知时展示当天可能范围，不展示上升、宫位或精确相位。城市列表是有限目录，可手动填写坐标及 IANA 时区。
- 手记保存在当前浏览器，可查看先前记录，不需要会员或登录。

## 当前就绪状态

截至 2026-09-21，本地代码包含新接口和自动验证。后端专项测试通过 30 项原接口测试和 13 项新增测试。测试中的 OpenAI／Redis 响应为模拟数据，不代表真实服务已经接通。

本次环境没有 OpenAI／Redis 凭据，配置检查会报告缺失；没有进行真实付费模型请求。当前可访问的 Vercel 团队查询中未找到对应项目，不能据此确认或更新既有的 marevys.vercel.app 生产环境。本文件不宣称新版本已上线。

| 能力 | 本地未配置凭据 | 生产需要 |
| --- | --- | --- |
| 页面、资料卡、手记 | 可用 | 静态资源部署 |
| 塔罗洗牌、翻牌、补牌 | 可用，进程内会话 | Redis 与稳定会话密钥 |
| 星盘计算、基础象征解读 | 可用 | Node.js 运行时及项目依赖 |
| 塔罗／星盘 AI 深入解读 | 明确显示不可用 | OpenAI、Redis、模型权限及限额配置 |

AI 不可用时仍可查看牌义或已计算的星盘，界面区分基础内容与 AI，不把本地文字冒充成功的 AI 解读。

## 本地运行

使用 Node.js 22。依赖固定在 package-lock.json。

```sh
npm ci
npm run build:web
npm run dev
```

访问 http://localhost:4173 。RC21 新体验需要 HTTP 服务；直接打开旧离线 index.html 或完整嵌入 HTML 不代表本版在线体验。

需要启用 AI 时，将 .env.example 复制为 .env.local，在本机私下填写凭据，然后运行：

```sh
npm run check:config
npm test
```

配置检查只验证变量存在和格式，不打印密钥，不调用供应商。测试不会产生 OpenAI 调用费用。真实服务验收见 [DEPLOYMENT.md](DEPLOYMENT.md)。

## 资料与解读边界

server/tarot-cards.json 包含 78 张牌的中／法／英原创释义摘要及出处；server/tarot-references.json 包含方法摘要、来源目录、PDF 页码和资料范围说明。当前状态为 curated_draft_not_professionally_reviewed：经过整理但尚未由专业同事进行案例盲评。

线上检索使用整理后的摘要和方法，不会每次发送整本书，也不等于已对模型完成训练。九份文件已登记来源；可提取程度、所属体系和完成审读程度不同。《神奇之门》属于奇门遁甲，不进入塔罗牌义检索。原始 PDF、全文提取及私有工作文件不进入公共网站资源。

牌面使用 RWS 1909 图像；署名和来源见 THIRD_PARTY_LICENSES/RWS_1909_ATTRIBUTION.md 及对应 provenance 文件。解读属于象征性探索，计算精度或书籍出处不构成预测准确性的验证。

## 数据保存

- 当前塔罗流程使用 sessionStorage，刷新同一标签页可继续；关闭标签页后的会话保留由浏览器决定。
- 点击保存后的解读、出生信息和手记使用 localStorage，最多保留 100 条新版本记录。清除浏览器数据会删除记录，没有跨设备同步。
- 生产 Redis 保存加密的塔罗牌堆、问题、背景和会话状态，最长 7 天，不保存 AI 正文。限额使用经过 HMAC 处理的 IP 标识，不写入原始 IP。
- AI 请求使用 store:false；这不等于供应商零数据保留承诺。应用不会主动将问题或出生信息写入日志。
- 会话令牌可以访问本次抽牌，应作为私有内容处理，不能粘贴到公开日志或链接。

## 主要文件

| 文件 | 职责 |
| --- | --- |
| src/reading-studio.js、.css | 塔罗、资料卡、手记及首页入口 |
| src/astrology-studio.js、.css | 出生信息、星盘及基础／AI解读 |
| api/tarot.js | 抽牌、一次补牌、Redis 状态控制 |
| server/tarot-engine.cjs | 牌阵、随机牌堆、加密会话 |
| api/reading.js | AI 配置、限额及 v1／v2 分发 |
| server/deep-reading.cjs | 资料检索、提示词、输出与出处校验 |
| api/astrology.js、server/astrology.cjs | 本命盘确定性计算 |
| api/locations.js | 有限城市目录查询 |
| scripts/build-web.cjs | RC21 构建及公开字段白名单 |
| vercel.json | 构建目录、函数依赖与超时 |
| DEPLOYMENT.md | 环境变量、上线与真实验收 |
| AI_INTEGRATION.md | v2 接口契约及兼容性 |

历史离线脚本和 v1 接口保留以兼容旧记录；旧版本的 22 张大牌、固定七日塔罗及会员门槛不再描述 RC21 主流程。
